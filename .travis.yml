language: php
php:
  - 5.6
sudo: false
branches:
  only:
    - master
script:
  - rbenv versions
  - rbenv install 2.4.0 ; rbenv global 2.4.0 ; gem install tw
  - '[ "$TRAVIS_PULL_REQUEST" == "false" ] && bash ./deploy.sh'
cache:
  directories:
    - .sculpin
    - ~/.rvm
