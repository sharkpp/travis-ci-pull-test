language: php
php:
  - 5.6
rvm:
  - 2.4.0
sudo: false
branches:
  only:
    - master
script:
  - rvm list
  - gem install tw
  - '[ "$TRAVIS_PULL_REQUEST" == "false" ] && bash ./deploy.sh'
cache:
  directories:
    - .sculpin
    - ~/.rvm
